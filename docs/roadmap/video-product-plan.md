# Scribix 产品 plan：从素材到持续发布

创建：2026-09-08；更新：2026-09-15。本文是视频产品功能状态与下一步优先级的统一入口，合并原 36 项功能建议与本次按用户流程梳理的清单。现迁至 roadmap 作为统一入口；旧 P0–P4 编号不再表示执行顺序。

目标：帮助持续更新播客、访谈和知识内容的个人及小团队，把一期内容做成一组完整、风格一致、可发布的短视频。竞品沿连续工作流扩展会有功能重叠；我们的验证重点是内容完整性、人工修改时间和下一次真实使用。

2026-09-17 批量选片专项（本地实现，默认关闭，未部署，三小时真实素材未验收）：[AI Clips 生成与交付实施方案](ai-clips-generation-workflow.md)记录 180 分钟分析范围、资源预算、持久化任务、动态候选和按需预览；本阶段只包含单条编辑与导出。验证证据见 [后台分析验证记录](../video-workspace/ai-analysis-validation.md)。

## 状态口径与依据

- **已有**：本地代码已有用户流程，仍需按对应技术记录核对验收和部署。
- **部分已有**：已有基础，但尚不能完成表中的整个用户任务。
- **已接入／待验收**：已有外部服务适配，不等于真实授权、发布或生产可用。
- **未实现／未接入**：后续候选范围；写入 plan 不表示立即全部开发。

本次核对本地代码与截至 2026-09-13 的实施记录，未查询远程部署或平台审核状态。Scribix 新发布流程在记录中仍为本地实现；ClipFlight / Teleo 的部分部署不能证明 Scribix 已上线。套餐、额度、保留期限及视频时长合同未因本 plan 改变。

实现依据：[选片](../../lib/video-workspace/selection.ts)、[代码合同](../../lib/video-workspace/contracts.ts)、[编辑](../video-workspace/editing-and-framing.md#editing)、[字幕与样式](../video-workspace/editing-and-framing.md#styles)、[发布准备](../video-workspace/publish-preparation.md)、[社交发布](../video-workspace/social-publishing.md)、[发布界面接入边界](../../app/components/publishing/README.md)。竞品证据保留在 [2026-09-08 调研](../research/2026-09-08-clipping-competitor-product-research.md)，其中本地差距是当日快照。

2026-09-15 编辑／发布入口调整：统一视频编辑区；字幕工具包含开场标题，封面工具包含封面文字；底部提供「下载」「发布」。发布跳转到独立页面并带入当前视频，必要时自动生成。账号复选框选择发布目标，平台标签仅切换文案与专属设置；一次提交到多个平台，文案分别保存，重试跳过已成功提交的平台。此前的弹窗／单平台提交方案已被此流程替代。提交后直接进入 History，同一次操作按任务合并展示各平台进度、文案与重试；侧栏统一使用 Channels，Publishing 内只保留 New post / History。localhost 提供不上传、不发帖的演示。开发验证未额外执行真实平台发布；定时发布与平台封面上传仍不在已接入范围内。

## 1. 按流程的功能清单

### 导入素材

| 功能 | 状态 | 当前边界／下一步 |
| --- | --- | --- |
| 本地视频上传、项目创建、处理状态 | 已有 | 完成真实新项目全流程验收 |
| 自动转写与时间定位 | 已有 | 支撑选片、字幕和词边界编辑 |
| YouTube 等链接导入原视频 | 未接入 | 优先一种目标用户常用来源；已有字幕导入不等于原视频导入 |
| 连接频道、自动导入新一期 | 未接入 | 普通来源导入成熟后增加更新检测与处理规则 |
| 节目与素材库管理 | 部分已有 | 有项目和草稿；缺节目归类、收藏归档、跨项目检索；不承诺永久存储 |

### 选片

| 功能 | 状态 | 当前边界／下一步 |
| --- | --- | --- |
| AI 推荐与完整性复审 | 已有 | 允许零候选；验证独立可理解、重复主题与采用率，并根据采用数量和主题覆盖判断当前候选数量是否足够 |
| 按主题和内容类型找片 | 已有 | 首次支持建议、观点、故事、问答等讲话内容条件 |
| 推荐理由与候选预览 | 已有 | 展示可核对理由，不把分数包装成爆款概率 |
| 从原视频手动创建片段 | 本地实现 | Original video 工作区提供连续段落转写、短语搜索、按词定位、文字范围与双手柄／精确起止时间选择；可追加独立片段，最多 90 秒，沿用付费编辑权限；不依赖 AI 候选 |
| 用户选择目标时长 | 本地实现 | Auto 15–90 秒，另有 15–30、30–60、60–90 秒；复审后服务端校验档位，手动编辑最多 90 秒 |
| 生成前外观设置与结果审阅 | 本地实现 | 字幕样式、开场标题、构图；默认列表／大预览、网格、收藏／排除、筛选排序、原文范围与独立编辑入口；见 [流程与验证](../video-workspace/clip-workflow.md) |
| 同一素材继续找片、换主题找片 | 部分已有 | 零匹配允许一次调整；成功产出后不能换方向；后续需保留草稿、去重并明确额度 |
| 按说话人筛选 | 未实现 | 不能把画面跟随当成说话人内容筛选 |

### 编辑

| 功能 | 状态 | 当前边界／下一步 |
| --- | --- | --- |
| 起止调整、转写词边界定位 | 已有 | 按原视频时间修改 |
| 字幕纠错与样式 | 已有 | 文字、模板、颜色、位置；继续验证准确性与可读性 |
| 自动构图、说话人跟随、人工裁切 | 已有 | 支持完整画面适配和分时段构图调整；真实素材可靠性仍需验收 |
| 预览、自动保存、独立草稿与恢复 | 已有 | 确认保存冲突、预览与导出一致、失败恢复 |
| 内部删句、拆分与重排 | 未实现用户流程 | 底层多段兼容不等于编辑器已开放；需同步字幕与预览时间 |
| 双人分屏、人物＋屏幕布局 | 未实现 | 首批素材以访谈或教程为主时提前 |
| 项目内同词批量纠错 | 未实现 | 优先修正嘉宾、产品和品牌拼写 |
| 停顿、口头禅、重复表达清理 | 未实现 | 从可预听、可撤销的长静音清理开始，依赖多段编辑 |
| 音量平衡与轻量降噪 | 未实现 | 当前无用户音频控件；验证原声与处理后失真 |

### 包装与交付

| 功能 | 状态 | 当前边界／下一步 |
| --- | --- | --- |
| 开头标题生成、选择、编辑、关闭 | 已有 | 有标题备选、样式及显示时间 |
| 封面选帧与独立文字排版 | 已有 | 从原素材生成独立封面；平台是否接受外部封面另行验证 |
| 发布标题、正文、标签及复制 | 已有 | 根据最终片段生成基础文案；尚非多套平台文案生成器 |
| 内容修改后提醒核对文案 | 已有 | 保留人工编辑，提示内容关联变化 |
| Logo 与单片品牌样式 | 已有 | 单片／项目控制；不等于跨项目预设 |
| MP4、JPG、文案 ZIP 与单项下载 | 已有 | 当前片段版本一致的交付与部分材料失败恢复；免费沿用原始候选导出权限 |
| 多比例输出与平台安全区 | 部分已有 | 现有 9:16 和通用安全区；缺更多比例及平台专属适配 |
| 用户素材 B-roll | 未实现 | 先允许截图、图片或录屏覆盖对应语句，再评估自动素材 |
| 背景音乐与简洁 CTA | 未实现 | 根据节目需求选择，不默认添加 |
| 翻译字幕与双语视频版本 | 未接入剪辑流程 | 独立转写产品的翻译不等于视频字幕翻译；配音另行评估 |

### 重复与批量制作

| 功能 | 状态 | 当前边界／下一步 |
| --- | --- | --- |
| 保存节目／品牌预设 | 未实现 | 保存字幕、Logo、颜色、开头标题和封面样式，下次复用 |
| 多条片段批量应用样式 | 未实现 | 保留每条内容、剪辑边界及明确保留的单片调整 |
| 多条批量导出与整组交付 | 未实现用户流程 | 单条材料 ZIP 不等于多条成片批量打包 |
| 跨项目品牌词库与纠错记忆 | 未实现 | 建立在项目内纠错基础上，允许查看、更改和删除 |

### 发布与持续运营

| 功能 | 状态 | 当前边界／下一步 |
| --- | --- | --- |
| 独立账号管理与发布页 | 已接入；付费门槛已部署 | Pro／历史付费 Basic 可连接账号、发布和排期；Free 可浏览示例工作区，付费操作原页打开 pricing modal，侧栏显示 PRO；API 校验当前套餐；付费限制已于 2026-09-18 部署，见社交发布记录 |
| YouTube 发布 | 已接入／待完整验收 | 有负责人确认公开发布验证的记录；补齐 Scribix 新项目全流程证据 |
| LinkedIn 个人主页视频发布 | 已接入／待真实验收 | 外部适配已有部署记录，真实 OAuth 与视频发布未确认 |
| TikTok 发布 | 已部署／待真实发布验收 | 2026-09-17 Direct Post 已批准，生产集成与开关已部署；账号授权及真实发布由用户验收，见 [部署记录](../video-workspace/tiktok-production-release-2026-09-17.md) |
| Instagram 等更多平台 | 未接入可用流程 | 图标和类型定义不算接入；按实际渠道需求扩展 |
| 发布历史、状态、安全重试 | 已接入／待验收 | 当前最近 30 条记录；不确定的平台初始化不能盲目重试 |
| 账号断开、重新连接与访问刷新 | 部分已有 | 已有入口；手动刷新仍有外部服务部署与真实续期待办 |
| 定时发布与内容日历 | 未接入 | 外部服务字段不等于 Scribix 已开放排期 |
| 自动导入 → 处理 → 按规则分发 | 未实现 | 依赖来源导入、稳定成片、排期与失败处理 |

### 复盘、协作与条件性扩展

| 功能 | 状态 | 启动条件 |
| --- | --- | --- |
| 播放、互动等平台表现回看 | 未接入 | 稳定发布且取得授权后；产品操作事件不能替代平台数据 |
| 表现反馈选片、标题／包装版本比较 | 未实现 | 足够同账号样本；不以模型评分宣布赢家或因果关系 |
| 分享预览、客户批注、审批 | 未实现 | 出现反复交给他人审核的任务 |
| 团队权限、多品牌和客户空间 | 未实现 | 明确团队或代运营客户需求 |
| 专业编辑器交接 | 未接入 | 真实用户经常转专业后期，先验证一种格式及兼容边界 |
| 视频转短帖、文章、Newsletter | 未形成完整复用流程 | 目标用户确实运营文字渠道；现有 AI Notes 不等于整套社交内容复用 |
| API、Webhook 与自动化集成 | 未提供对外产品流程 | 从明确客户的一个输入输出流程起步；内部 API 不算对外能力 |
| 跨段主题合集与预告片 | 未实现 | 多段编辑成熟，组合不改变原意 |
| 素材库／AI B-roll、视觉事件选片 | 未实现 | 用户素材覆盖被采用后评估自动素材；体育、游戏、Vlog 各自建立质量样本 |

AI 分身、眼神修正、声音克隆／口型配音、通用视频 Agent、社交收件箱、完整录制与直播平台仍为观察项，不列入承诺开发范围。

## 2. 当前推进顺序与完成标准

| 阶段 | 要完成的工作 | 完成标准 |
| --- | --- | --- |
| A：验收并交付已有主流程 | 新项目上传 → 按意图选片 → 编辑 → 标题／封面／文案 → 下载；验证已接入平台 | 真实账号完成全流程，材料可打开且版本一致，免费路径不回归；平台授权、视频链接和状态有证据；按部署清单逐项确认远程依赖 |
| B：提高重复制作效率 | 节目预设 → 批量应用 → 多条导出与打包 | 同一期选中多条，统一样式后整组交付；下一期可复用，单片内容和调整不被误覆盖 |
| C：减少实际修片障碍 | 继续找片、时长选择、批量纠错；按素材需要增加内部删句、双人／屏幕布局 | 用相同素材记录采用率、主动修改时间、裁切／字幕问题与每条可用成本，依据主要障碍排序 |
| D：持续运营 | 常用来源导入、定时发布，再做自动导入和按规则分发 | 至少一种来源及目标平台能持续运行；授权失效、失败、重试、重复提交和排期状态清楚 |
| E：有证据后扩展 | 平台效果分析、团队协作、更多内容形式和平台 | 有持续使用和具体客户任务，不为完成功能清单而开发 |

A 中遇到候选不完整、画面不可用或导出故障，先修复阻断问题；已经验证通过的基础不重复开发。B 与 C 可按真实用户障碍调整先后。社交发布已经接入，不再安排一次“从零接入”；排期与自动化仍属后续范围。

本次仅更新计划和文档，不执行部署、远程迁移、平台发布或外部联系。后续实施保持当前权限、套餐和追踪合同，功能变更时再同步对应技术文档。

## 阶段 A 当前进度（2026-09-15）

已用用户提供的约 59 分钟视频在 localhost:3000 完成真实上传、转写、5 条自动候选、预览、第一条标题／封面／文案、修改保存恢复与成片生成。测试没有重复修改现有历史项目。

实际下载由用户接手验收，日志中的下载连接中断仍待确认；五条内容与构图质量、免费路径、移动端、新手任务和真实平台发布未全部验收。阶段 A 保持进行中，不标记完成。细节和测试项目入口见[验收记录](../video-workspace/publish-preparation.md)。

同日发现普通 Google OAuth 登录将 Google sub 覆盖为 Auth.js 临时 UUID，导致发布白名单误判、侧栏入口隐藏。已修正 JWT 身份优先级，Google OAuth 与 One Tap 的身份回归测试通过；旧会话需重新登录以修正身份。随后按产品决定移除用户 ID 白名单：配置发布服务后，所有登录用户都可进入发布，本地与生产规则一致；登录与资源归属校验保留。

2026-09-15 追踪修复：已定位本地 MediaPipe AVX/SIGILL 兼容性故障，补充分析日志与实际推理构建检查，五条预览重新分析及五条成片／封面生成均完成。第一条网页预览已确认聚焦中间人物；第 4/5 条仍有部分全景时段，完整追踪质量不标记已通过。详见[追踪验收记录](../video-workspace/editing-and-framing.md#2026-09-15-local-cpu-compatibility-and-failure-diagnostics)。

## 3. 验证和维护

- 记录独立完整的候选比例、愿意发布的比例、人工修改时间、处理等待、每条可用成片成本，以及下一次真实使用。
- 下载只证明发起下载；实际发布以平台结果或用户自愿反馈确认。沿用 [tracking](../video-workspace/tracking.md)，不新增追踪数据库或回放设施。
- [首期方案](../archive/plans/publish-preparation-design.md) 保留选片与单片发布准备的交互决策和验收要求，不再维护另一份功能状态表。
- 部署前读 [外部检查清单](../video-workspace/operations.md#deployment)，平台状态与验收证据读 [社交发布记录](../video-workspace/social-publishing.md)。本表仅在有代码或验收证据时更新状态，记录日期。
- SEO、博客、获客和 Google Ads 保留各自调研；本计划不自动开启投放。

2026-09-16 原视频选片第一阶段：项目提供 Original video / Clips 入口；原视频范围选定后才创建手动 clip，取消短视频打开后自动创建默认片段。用户可追加多个手动片段，AI 候选保存保留手动片段。此段为第一阶段记录；生成前长度设置与结果审阅已在后续本地实现，见 [当前流程](../video-workspace/clip-workflow.md)。成功后反复换主题仍未实现。使用既有测试视频创建了 00:06.12–00:11.10 的手动测试片段，原有五条候选保留。

### 2026-09-16 workspace / Planner update

Home now contains upload and recent projects; Projects is a separate library. Sidebar separates Create and Social media (Posts, Planner, Channels). Manual source selection is secondary. The subsequent local generation settings and review workflow are documented in [clip-workflow.md](../video-workspace/clip-workflow.md).

Planner and schedule submission are implemented locally together with Teleo's external schedule bridge. Both apps still need the normal release process; real scheduled publishing from Scribix has not been accepted against production. Month view, mobile list, rescheduling and cancellation are included; Teleo's week view, drag/drop and full scheduled-content editing remain outside this first integration.
